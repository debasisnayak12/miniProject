import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './Skills-manage/App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <>
    <App />
  </>
);
